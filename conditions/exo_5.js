let totalMinutes = 130;
let hours = Math.floor(totalMinutes / 60); // Calcul des heures entières
let minutes = totalMinutes % 60; // Minutes restantes avec l'opérateur modulo

console.log(totalMinutes + " minutes représentent " + hours + " heures et " + minutes + " minutes.");
